import React from 'react'

/**
 * @description Returns a JSX element representing a basic HTML div, implying it is
 * a React functional component designed to render a page for creating zap actions.
 *
 * @returns {ReactElement} An object representing a DOM node that can be rendered to
 * the screen, specifically a <div> element.
 */
const ZapCreatePage = () => {
  // 1. trigger
  // 2. actions[]
  return (
    <div>
        
    </div>
  )
}

export default ZapCreatePage